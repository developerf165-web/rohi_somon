/**
 * Configuration for theme-related features.
 */
export const themeConfig = {
  /**
   * If true, the theme toggle button will be visible at the top of the screen.
   * Set this to false for production or to hide the toggle.
   */
  showThemeToggle: true,
};
